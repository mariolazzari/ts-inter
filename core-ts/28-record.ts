type Names = Record<string, string>;

const names: Names = {
  mario: "Mario",
  maria: "Maria",
};

type NumericRecord = Record<string, number>;

const salary: NumericRecord = {
  annual: 50000,
  bonus: 10000,
};
