const myName: string = "Mario";
const age: number = 30;
const isAdmin: boolean = true;

const user: {
  name: string;
  age: number;
  isAdmin: boolean;
} = {
  name: myName,
  age,
  isAdmin,
};

const myNumbers: number[] = [1, 2, 3, 4, 5];
